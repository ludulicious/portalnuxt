services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: portal_saas
      POSTGRES_PASSWORD: postgres
    ports: ['54329:5432']
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres -d portal_saas']
      interval: 5s
      timeout: 3s
      retries: 10
